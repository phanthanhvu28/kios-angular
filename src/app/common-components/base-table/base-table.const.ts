import { TableNavConfig } from '@models/base-data-list';

export const DEFAULT_NAV_CONFIG: TableNavConfig = {
  nvShowSearch: true,
  nvSearchPosition: 'left',
  nvShowFilter: true,
  nvShowDefaultFilter: true,
  nvShowHideCols: true
};
